# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-zero-distro"
BBFILE_PATTERN_meta-zero-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-zero-distro = "6"

LAYERDEPENDS_meta-zero-distro = "core"
LAYERSERIES_COMPAT_meta-zero-distro = "warrior"
